import React, {Component} from 'react';
import {Text, View} from 'react-native';

export class contact extends Component {
  render() {
    // const {email} = this.props.route.params;
    return (
      <View>
        <Text> nama anda adalah: {} </Text>
      </View>
    );
  }
}

export default contact;
